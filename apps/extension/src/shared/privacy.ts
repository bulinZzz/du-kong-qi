/**
 * 隐私声明的措辞。
 *
 * 同一件事要在两个地方说：浮窗里第一次把文字发出去时的那一块，以及设置里的声明入口。
 * 两处说法必须一致——一处改了另一处没改，用户读到的就是两个版本，所以措辞集中在这里。
 *
 * brief 是完整声明里 when 与 judgement 两句的压缩：浮窗里那次只放得下一句，
 * 改其中一句时顺手看一眼另一句。
 */
export const PRIVACY_TEXT = {
  /** 声明的标题，两处都用它。 */
  title: '隐私声明',
  /** 最短的一句：文字去了哪、谁在判断、我们这侧留不留。浮窗里那次提示只放它。 */
  brief: '分析时会把网页里的讨论文字发送到服务器，再交给第三方模型服务判断；服务器不保存这些文字。',
  /** 什么时候会发。 */
  when: '读空气只在你让它分析、或打开一个你授权过的网站时，把网页里的讨论文字发送到自己的服务器。',
  /** 谁在判断，以及我们这一侧留不留。 */
  judgement: '判断由第三方模型服务完成，服务器不保存这些文字。',
  /** 最后一环不由我们决定，得照实说：说清不知道的是哪一环，而不是把话停在"不归我管"。 */
  retention: '模型服务那一环是否留存，由它的政策决定，我们无从确认。',
  /** 回程：用户能怎么办。声明只说我们会怎样，等于让用户无从下手。 */
  control: '不想让它自动发送时，可以在设置里关掉。',
} as const;
